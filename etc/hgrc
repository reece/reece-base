# Reece's .hgrc

[alias]
authors = !hg log --template "{author}\n" | sort -u
qapplied = qapplied -s
qdiff = diff -r qparent
qmeld = meld -r qparent
qst = qapplied -s
qunapplied = qunapplied -s
removed-files = log --template "{rev}: {file_dels}\n"

[extensions]
bookmarks=
churn=
color=
convert=
extdiff=
fetch=
graphlog=
git = 
histedit=
#inotify=
mq=
pager=
progress=
prompt = ~/opt/reece-external/lib/hg/hg-prompt/prompt.py
rebase=
record=
transplant=

[extdiff]
cmd.meld = 


[hooks]
# show stats of incoming changes
changegroup = hg diff --stat -r $HG_NODE -r tip


[merge-tools]
#emacs.args = -q --eval "(ediff-merge-with-ancestor \"$local\" \"$other\" \"$base\" nil \"$output\")"
#emacs.args = -q --eval "(emerge-files-with-ancestor nil \"$local\" \"$other\" \"$base\" \"$output\" nil 'kill-emacs)"
#meld.args = $local $base $other $output

[pager]
pager = LESS='FSRX' less
ignore = version, help, update, serve, record


[tortoisehg]
editor = gedit
vdiff = meld
monitorrepo = always
authorcolor = True


[trusted]


[ui]
username = Reece Hart <reecehart@gmail.com>
portablefilenames = ignore
#merge = emacs
#merge = emacsclient-merge


#[web]
#cacerts = /etc/ssl/certs/ca-certificates.crt


[revsetalias]
# revisions between [$1,$2), $1 a descendent (newer than) $2
# This is different from $1:$2, which is not ancestry-aware
btw($1, $2) = $1:$2 & ancestors($1) - $2
