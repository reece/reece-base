# Reece's .hgrc

[alias]
removed-files = log --template "{rev}: {file_dels}\n"
qdiff = diff -r qparent
qmeld = meld -r qparent
qapplied = qapplied -s
contributors = !hg log --template "{author|person}\n" | sort | uniq -c | sort -nrs

[extensions]
bookmarks=
churn=
color=
convert=
extdiff=
fetch=
#git=
graphlog=
#inotify=
mq=
pager=
progress=
rebase=
record=
transplant=

# pip install hg-github
#hggithub=

#hggit = /home/reece/opt/hg-git/hggit

# pip install hg-github
#hggithub=

hggit = /home/reece/opt/hg-git/hggit


[extdiff]
cmd.meld = 


[hooks]
# show stats of incoming changes
changegroup = hg diff --stat -r $HG_NODE -r tip


[merge-tools]
#emacs.args = -q --eval "(ediff-merge-with-ancestor \"$local\" \"$other\" \"$base\" nil \"$output\")"
#emacs.args = -q --eval "(emerge-files-with-ancestor nil \"$local\" \"$other\" \"$base\" \"$output\" nil 'kill-emacs)"


[pager]
pager = LESS='FSRX' less
ignore = version, help, update, serve, record


[tortoisehg]
editor = gedit
vdiff = meld
monitorrepo = always


[trusted]
users = locusadm


[ui]
username = Reece Hart <reecehart@gmail.com>
#merge = emacs
#merge = emacsclient-merge


#[web]
#cacerts = /etc/ssl/certs/ca-certificates.crt


